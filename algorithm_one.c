#include "algorithm_one.h"
#include "structure.h"

int enumerationMaxSubarray(struct dynArr *da){
    int i;
    int j;
    int k;
    int sum;
    int maxSum = 0;
    for(i = 0; i < da->size; i++){
      for(j = 0; j < da->size; j++){
        for(k = j; k < da->size; k++){
          
        }
      }
    }
}
